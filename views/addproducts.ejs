<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Product | GameVault</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/stylesproduct.css">
</head>
<body class="bg-gray-900 text-white flex flex-col min-h-screen">

  <!-- Navigation -->
    <nav class="navbar mb-10">
        <div class="nav-container">
            <div class="logo">🎮 GameVault</div>
            <button class="hamburger" id="hamburger-btn" aria-label="Open Menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
            <ul class="nav-links" id="nav-links">
                <li><a href="/"><span>Home</span></a></li>
                <li><a href="/product"><span>Products</span></a></li>
                <li><a href="#features"><span>Features</span></a></li>
                <li><a href="#newsletter"><span>Offers</span></a></li>
                <li><a href="#contact"><span>Contact</span></a></li>
                <li><a href="/login" class="login-btn"><span>Login</span></a></li>
            </ul>
        </div>
    </nav>
  
  <!-- Flash Message -->
    <% if (success.length > 0) { %>
        <div class="gamevault-message success">
            <span class="msg-icon">✅</span>
            <span class="msg-text"><%= success %></span>
            <span class="msg-close" onclick="this.parentElement.style.display='none';">&times;</span>
        </div>
    <% } %>
    <% if (error.length > 0) { %>
        <div class="gamevault-message error">
            <span class="msg-icon">❌</span>
            <span class="msg-text"><%= error %></span>
            <span class="msg-close" onclick="this.parentElement.style.display='none';">&times;</span>
        </div>
    <% } %>


  <!-- ✅ Main Upload Form -->
  <main class="flex-grow flex items-center justify-center mt-10 z-1000 px-4">
    <div class="bg-gray-800 p-8 rounded-2xl shadow-2xl w-full max-w-lg my-12">
      <h2 class="text-2xl font-bold text-center mb-6">📦 Upload New Product</h2>

      <form action="/products/addproduct" method="POST" class="space-y-5">
        <!-- Product Name -->
        <div>
          <label class="block mb-1 font-medium">Product Name</label>
          <input type="text" name="name" required class="w-full p-3 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500">
        </div>

        <!-- Brand -->
        <div>
          <label class="block mb-1 font-medium">Brand</label>
          <input type="text" name="brand" required class="w-full p-3 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500">
        </div>

        <!-- Category -->
        <div>
          <label class="block mb-1 font-medium">Category</label>
          <select name="category" required class="w-full p-3 rounded-lg bg-gray-700 border border-gray-600 text-white">
            <option value="">Select Category</option>
            <option value="Headset">Headset</option>
            <option value="Controller">Controller</option>
            <option value="Keyboard">Keyboard & Mouse</option>
            <option value="Monitor">Monitor</option>
            <option value="Chair">Chair</option>
          </select>
        </div>

        <!-- Price -->
        <div>
          <label class="block mb-1 font-medium">Price (INR)</label>
          <input type="number" name="price" min="1" required class="w-full p-3 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500">
        </div>

        <!-- Description -->
        <div>
          <label class="block mb-1 font-medium">Description</label>
          <textarea name="description" rows="3" required class="w-full p-3 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 resize-none"></textarea>
        </div>

        <!-- Image Upload -->
        <div>
          <label class="block mb-1 font-medium">Product Image Link</label>
          <input type="link" name="image" min="1" required class="w-full p-3 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500">
        </div>

        <!-- Submit Button -->
        <div class="pt-4">
          <button type="submit" class="w-full bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-lg font-semibold transition duration-200">
            Upload Product
          </button>
        </div>
      </form>
    </div>
  </main>

  <!-- ✅ Footer -->
  <footer class="bg-gray-950 text-gray-400 py-6 text-sm mt-auto">
    <div class="max-w-7xl mx-auto px-6 flex flex-col sm:flex-row justify-between items-center gap-4">
      <p>&copy; <%= new Date().getFullYear() %> GameVault. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
